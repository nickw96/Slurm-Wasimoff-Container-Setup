[Unit]
Description=Starting the wasimoff broker
#After=
Before=slurmctld.service

[Service]
ExecStart=/usr/bin/bash /bin/start_broker.sh
#ExecStop=
#ExecReload=
Type=simple
#Restart=

[Kill]
KillMode=control-group
KillSignal=SIGTERM

[Install]
WantedBy=default.target
#RequiredBy=slurmctld.service